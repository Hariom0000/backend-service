spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  sql:
    init:
      mode: always

server:
  port: 8080

app:
  cors-allowed-origin: ${APP_CORS_ALLOWED_ORIGIN:http://localhost:5173}
